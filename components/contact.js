import styles from 'styles/contact.module.css'

export default function Contact () {
  return (
    <div classNamei={styles.stack}>
      <h3 className={styles.heading}>Contact</h3>
      <address>cube@web.mail.address</address>
    </div>
  )
}
